# Production environment settings
# Replace with your actual production values

# Base URL for your production site - must match Azure AD redirect URI configuration
REACT_APP_BASE_URL=https://nitor-smartdocs.azurewebsites.net/

# Public URL path - leave as '/' if deployed to root, or set to '/appname' if in a subdirectory
PUBLIC_URL=/

# API URL if different from base URL
REACT_APP_API_URL=https://nitor-smartdocs.azurewebsites.net/api

# Logging level for production (0=Error, 1=Warning, 2=Info, 3=Verbose)
# Recommend 1 for production
REACT_APP_LOG_LEVEL=1

# Application version - IMPORTANT: Increment this value for each deployment to bust cache
REACT_APP_VERSION=1.0.1 

NODE_ENV=production